#include "gravitysim3D.ih"

void Gravitysim3D::renderImg(Image &img)
{
    /*
    2D rendering version:
    
    for (Star star : d_stars)
    {
        if (star.d_pos.x > 0 && star.d_pos.x < SIZE && star.d_pos.y > 0 && star.d_pos.y < SIZE)
            img.setPixel(star.d_pos.x, star.d_pos.y, sf::Color(255, 255, 255));
    }*/
    
}